# Fonction serverless netlify/functions/contact.js — à définir dans Netlify (Site settings > Environment variables),
# jamais commitées dans le repo.
SMTP_HOST=ssl0.ovh.net
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=contact@marc-dev.fr
SMTP_PASS=
CONTACT_TO=contact@marc-dev.fr
